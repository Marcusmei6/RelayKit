interface:
  display_name: "RelayKit Desktop Query"
  short_description: "Run one planned Desktop model validation query"
  default_prompt: "Use $relaykit-desktop-query only when RelayKit's validation plan selects live-desktop-query, with explicit current catalog and artifact hashes."

policy:
  allow_implicit_invocation: false
